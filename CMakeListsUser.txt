set(CMAKE_VERBOSE_MAKEFILE ON)

add_custom_target(
    Upload ALL
    COMMAND platformio -c clion run --target upload "$<$<NOT:$<CONFIG:All>>:-e${CMAKE_BUILD_TYPE}>"
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)

if (CMAKE_BUILD_TYPE MATCHES "genericSTM32F427VGT")
endif()

if (CMAKE_BUILD_TYPE MATCHES "genericSTM32F427VGT")
endif()